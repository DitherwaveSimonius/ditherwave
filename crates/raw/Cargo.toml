[package]
name = "ditheros-raw"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ditheros-core.workspace = true
thiserror.workspace = true
# LGPL-3.0-only. Used as an unmodified, dynamically-swappable dependency only —
# see the licensing note in README.md. Wraps rawloader's sensor decode with
# demosaicing and color processing, which we don't want to hand-roll.
imagepipe = "0.5"
