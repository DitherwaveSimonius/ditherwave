[package]
name = "generate-avatar"
version = "0.1.0"
edition = "2021"

# Deliberately its own workspace root — see branding/README.md.
[workspace]

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }
